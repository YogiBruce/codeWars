function updateLight(current) {
    return ({green: 'yellow', yellow: 'red', red: 'green'})
    [current]
  }

  function updateLight(current) {
    return current === 'yellow'? 'red': current === 'green' ? 'yellow' : 'green'
  }