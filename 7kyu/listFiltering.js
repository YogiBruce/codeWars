//write a function that returns an array with numbers filtered out

const filter_list = l => l.filter(n => typeof n == 'number')

function filter_list(l) {
    return l.filter(n => typeof n == 'number')
  }

  const filter_list = l => l.filter(n => typeof n == 'number')